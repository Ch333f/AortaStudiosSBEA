services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"

  api:
    build: .
    depends_on:
      - redis
    ports:
      - "9000:8000"
    environment:
      REDIS_URL: ${REDIS_URL}
    volumes:
      - ./:/server
    command: uvicorn server.main:app --host 0.0.0.0 --port 8000
